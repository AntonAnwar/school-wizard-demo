# مواصفات تصميم: إنشاء الصفوف والشعب (معالج 4 خطوات)

> حل سؤال: ما أفضل طريقة لإنشاء الصفوف والشعب بواجهة سهلة، مع تعيين مربّي صف لكل شعبة وتحديد الحد الأقصى لكل شعبة، بشكل عملي على هيئة معالج (Wizard) **دون إدخال متكرر من لوحة المفاتيح**؟

---

## 1) الفكرة الجوهرية

**معالج (Wizard) من 4 خطوات** يعتمد على ثلاث آليات تقضي على الكتابة:

1. **البنية جاهزة مسبقًا** — قوالب لكل مرحلة + «نسخ من العام الماضي» (الافتتاحي الموصى به).
2. **الأرقام بأزرار** — عدّادات (Steppers) للحد الأقصى مع «تطبيق على الكل»، وأسماء الشعب (أ، ب، ج…) آلية.
3. **المعلمون ببطاقات** — فلاتر ونقر وتعيين جماعي، بدل كتابة أسماء.

النتيجة: **0–1 حقول نصية** في كامل المسار (البحث عن معلّم في قائمة طويلة فقط)، وزر إنشاء واحد يعمل بعملية ذرّية.

---

## 2) قصة المستخدم (User Story)

> كمدير مدرسة، في بداية كل عام دراسي، أريد أن أنشئ صفوف العام الجديد وشعبها، وأحدد الحد الأقصى لكل شعبة، وأعيّن مربّي صف لكل شعبة، **بنقرات قليلة على شاشة واحدة متسلسلة**، دون إعادة كتابة أسماء الصفوف والشعب والمعلمين، وأحصل على ملخص أراجعه قبل أن يصبح كل شيء رسميًّا في النظام.

---

## 3) المسار (Flow)

```
[صفحة إدارة الصفوف والشعب]
   └─ زر «إنشاء صفوف وشعب» (أو من حالة فارغة)
        ↓
  الخطوة ١: العام الدراسي + المستويات/الصفوف (رقاقات)
        ↓
  الخطوة ٢: مصدر البنية (قالب / عام سابق / من الصفر)
            + عدد الشعب لكل صف (أسماء آلية، +/−)
            + الحد الأقصى (عدّاد عام + تطبيق على الكل + تجاوز فردي)
        ↓
  الخطوة ٣: تعيين مربّي الصف (بطاقات معلمين + تعيين فردي/جماعي)
        ↓
  الخطوة ٤: المراجعة (جدول ملخص + فحوص + تعديل مباشر للسطر)
        ↓
  [إنشاء ذرّي] → [توست نجاح + تراجع 10 ثوانٍ] → [صفحة الإدارة بالشعب الجديدة]
```

**مسار الخطأ:** أي فحص يفشل في الخطوة ٤ يعيد المستخدم إلى الخطوة المعنيّة (٢ أو ٣) مع الحفاظ على كل المدخلات.

---

## 4) تفاصيل الشاشات

### شاشة 0 — صفحة إدارة الصفوف والشعب (نقطة الدخول)
- **العناصر:** ترويسة + زر «إنشاء صفوف وشعب» + تبويبات الأعوام + جدول (أو حالة فارغة).
- **الحالة الفارغة (سنة جديدة):** دعوة واضحة للمعالج — هي دخول ثانٍ.
- **القاعدة:** زر الإنشاء يظهر دائمًا في الترويسة؛ لا يمكن الوصول للمعالج من مكان آخر (مسار واحد واضح).

### شاشة 1 — الخطوة ١: العام والمستوى
| العنصر | السلوك |
|---|---|
| العام الدراسي (رقاقات) | الافتراضي = العام القادم. الأعوام التي فيها شعب معطّلة (تُدار من صفحة الإدارة). |
| المستويات/الصفوف (رقاقات متعددة) | نقرة لكل مستوى + «اختيار الكل». |
- **قاعدة التحقق:** ≥ مستوى واحد للمتابعة.

### شاشة 2 — الخطوة ٢: الشعب والحد الأقصى
| العنصر | السلوك |
|---|---|
| مصدر البنية (3 بطاقات) | **قالب جاهز** (تعرّفه الإدارة: عدد شعب لكل مرحلة + نمط أسماء + حد افتراضي) · **نسخ من العام الماضي** (موصى به) · **من الصفر**. |
| شريط الاقتراح | «يقترح النظام: … حسب تسجيل العام السابق» + زر «اعتماد الاقتراح». |
| صفوف المستويات | لكل مستوى: رقاقات شعب (أ، ب، ج… + ✕ + ＋) وعدّاد حد أقصى فردي. |
| الحد الأقصى العام | عدّاد + «تطبيق على جميع الشعب». |
- **قواعد:**
  - أسماء الشعب آلية دائمًا (لا حقل نصي).
  - الإضافة تحذف الحرف التالي تلقائيًا (أ→ب→ج→د).
  - الحد الأدنى للحد الأقصى = 1.
  - عدّاد حي أسفل التذييل: «سيتم إنشاء N شعبة في M صفوف».

### شاشة 3 — الخطوة ٣: تعيين مربّي الصف
- ** العمود الأول (المعلمون):** بطاقات (اسم، تخصص، عدد الشعب الحالي)، فلاتر رقاقات (تخصص/مرحلة)، بحث نصي اختياري، تنبيه ⚠ فوق الحد الموصى به.
- **العمود الثاني (الشعب):** سطور (الصف/الشعبة + معلّم معيّن ✓ | زر «تعيين» | حالة «جارٍ التعيين»).
- **التعيين الفردي:** اختيار سطر الشعبة → نقر بطاقة المعلّم (أو سحب وإفلات كبديل متقدم).
- **التعيين الجماعي:** «تحديد الكل» + اختيار معلّم واحد + «تعيين للمحددة».
- **المؤشر الحي:** 9/13 معيّن + شريط تقدم.
- **قواعد:**
  - «التالي» معطّل حتى 13/13، أو خيار صريح «إنشاء بدون مربّي» لكل شعبة متبقية (يُسجَّل كتحذير في المراجعة).
  - أعباء المعلم = عدد الشعب المعيّنة عليه في هذا العام + العام الحالي (حسب سياسة المدرسة)؛ تجاوز الحد الموصى به = تحذير غير حاسم.

### شاشة 4 — الخطوة ٤: المراجعة
- **قائمة الفحوص:** ✅ مكتمل / ⚠️ تحذير (أعباء) — كل تحذير له إجراء (عرض بدائل).
- **جدول الملخص:** الصف | الشعبة | مربّي الصف | الحد الأقصى | «تعديل» (يقفز للخطوة ٢ أو ٣ دون فقدان بيانات).
- **زر التأكيد:** «تأكيد وإنشاء (13 شعبة)».

### شاشة 5 — النجاح
- **توست:** «تم إنشاء 13 شعبة في 4 صفوف وتعيين 13 مربّي صف» + **تراجع (10 ثوانٍ)**.
- **صفحة الإدارة:** الصفوف الجديدة ظاهرة فورًا بشارة «جديد» + إحصاءات (مستويات/شعب/مربّون معيّنون).

---

## 5) الحالات الحدّية

| الحالة | السلوك |
|---|---|
| شعبة/صف موجود مسبقًا في نفس العام | تحذير قبل التأكيد + خيار «تخطي الموجود» |
| لا معلمين متاحين (مدرسة جديدة) | السماح بإنشاء الشعب، مع «إنشاء بدون مربّي» + تحذير واضح في المراجعة |
| أعباء معلّم فوق الحد الموصى به | تحذير غير حاسم + اقتراح بديل (أقرب معلّم بأعباء أقل) |
| عدد الشعب × الحد الأقصى < التسجيل المتوقع | تلميح إخباري: «قد تحتاج شعبة إضافية» |
| المستخدم ألغى المعالج | لا يُحفظ شيء — الإنشاء يتم مرة واحدة عند التأكيد |
| تراجع بعد الإنشاء | زر تراجع في التوست (10 ثوانٍ)؛ لاحقًا من صفحة الإدارة (حذف/تعديل) |
| تغيير الحد الأقصى العام بعد تخصيص بعض القيم | يسأل: «تطبيق على الكل؟» (يتجاوز المخصصات) أو «على غير المخصص فقط؟» |

---

## 6) نموذج البيانات المقترح

```
Stage              { id, name }                        // روضة، أساسية، ثانوية
GradeLevel         { id, name, stageId }               // الأول الأساسي…
AcademicYear       { id, label }                        // 2026/2027
Section            { id, yearId, gradeLevelId, name,    // «أ»، «ب»…
                     maxCapacity, homeroomTeacherId,
                     status, createdAt }
Teacher            { id, name, subject, maxRecommendedSections }
SectionTemplate    { stageId, sectionsPerGrade,
                     letterPattern, defaultMaxCapacity }
```

- **الإنشاء الذرّي:** INSERT جميع الشعب + التعيينات في معاملة واحدة؛ فشل أي جزء = استرجاع كامل.
- «نسخ من العام الماضي» = استعلام بنية `AcademicYear-1` (المستويات + عدد الشعب + الحد الأقصى + معلّمو العام إن لم يتغيروا) ثم عرضها قابلة للتعديل.

---

## 7) مقاييس النجاح (Metrics)

| المقياس | الهدف |
|---|---|
| زمن الإنشاء (13 شعبة / 4 مستويات) | < 2 دقيقة |
| نسبة المستخدمين المكملين دون أي كتابة | > 85% |
| نسبة إتمام المعالج دون تخلي | > 95% |
| أخطاء البنية بعد الإنشاء (تصحيحات يدوية) | < 2% |

---

## 8) نقل التصميم إلى فيجما

1. **html.to.design (موصى به):** Plugin في فيجما — يلصق محتوى `wizard-mockup.html` ويخرجه كإطارات قابلة للتحرير. اقطع الأقسام (00–05) كـ Frames باسمها.
2. **لقطات شاشة:** File → Place Image كقاعدة خلفية لكل Frame (للمراجعة السريعة).
3. **إعادة بناء يدوية:** وفق Tokens الموضحة في `wizard-mockup.html` (قسم المواصفات) — أنشئ مكونات: Chip, Stepper, Teacher Card, Section Row, Step Indicator, Toast.

**Tokens المختصرة:** Primary `#4F46E5` · Success `#059669` · Warning `#F59E0B` · Error `#DC2626` · نص `#111827` / ثانوي `#6B7280` · خلفية `#EEF1F6` · 8px grid · زوايا 14/10/999px · حدود 1.5px `#E5E7EB` · RTL كامل.
